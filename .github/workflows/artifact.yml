name: Artifact

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - name: Set up JDK 17
      uses: actions/setup-java@v6
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Set up Maven
      uses: stCarolas/setup-maven@v5
      with:
        maven-version: '3.9.9'
    - name: Build with Maven
      run: mvn -B clean verify -ntp -DskipTests
    - name: Archive plugin dist
      uses: actions/upload-artifact@v7
      with:
        name: plugin
        path: sites/com.tlcsdm.eclipse.ucdetector.site/target/*.zip
    - name: Archive update site
      uses: actions/upload-artifact@v7
      with:
        name: update_site
        path: sites/com.tlcsdm.eclipse.ucdetector.site/target/repository
