name: Eclipse Version Compatibility

on:
  pull_request:
    branches: [ master ]
    paths-ignore:
      - '**/*.md'
      - 'LICENSE'
      - '.gitignore'
      - '.gitattributes'
      - '.github/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  discover-targets:
    runs-on: ubuntu-latest
    outputs:
      targets: ${{ steps.targets.outputs.targets }}
    steps:
    - uses: actions/checkout@v7
    - name: Discover target files
      id: targets
      shell: bash
      run: |
        python - <<'PY'
        import json
        import os
        from pathlib import Path

        targets = sorted(path.stem for path in Path("targets").glob("*.target"))
        with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
            print(f"targets={json.dumps(targets)}", file=output)
        PY

  compatibility:
    needs: discover-targets
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJson(needs.discover-targets.outputs.targets) }}
    runs-on: windows-latest
    name: Eclipse ${{ matrix.target }}

    steps:
    - uses: actions/checkout@v7
    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: 'maven'
    - name: Set up Maven
      uses: stCarolas/setup-maven@v5
      with:
        maven-version: '3.9.9'
    - name: Build with Maven against Eclipse ${{ matrix.target }}
      run: mvn -B clean verify -ntp "-Dtarget.platform.file=targets/${{ matrix.target }}.target" "-Dtarget.resolve.with.ee.constraints=${{ startsWith(matrix.target, '2024-') && 'true' || 'false' }}"
